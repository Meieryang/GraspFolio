# 保存与跨设备迁移

## 使用方式

1. 两台设备均使用支持旁文件版本 2 的新版掌页。
2. 在原设备打开 PDF，通过系统返回键进入菜单，在设置中授权 PDF 所在目录。
3. 阅读或修改封面独页设置后，等待状态显示“已同步到 PDF 旁”。可以通过“重试同步”主动保存。
4. 将同名的 `Book.pdf` 和 `Book.graspfolio` 一起复制到另一台设备的同一个文件夹，保持二者文件名对应。
5. 在新设备打开 PDF，并授权其所在文件夹。应用校验 PDF 内容后加载笔迹、阅读页码和封面独页设置。

这是文件迁移，不包含云端自动同步。设备级笔刷偏好、振动和预测开关不随书籍迁移。书签、音频尚未实现，因此本版不迁移这些内容。

如果两端都已经产生未同步修改，应用会保留本地恢复数据并报告冲突，不自动选择一端覆盖另一端。初次导入时请先授权目录、完成载入，再继续阅读或书写。

## 保存中断保护

应用不再以清空旧旁文件的方式保存，而是：

1. 在应用内部原子记录本次保存意图，包括旧/新内容哈希、对应本地版本和唯一临时文件名。
2. 在 PDF 目录新建临时文件，完整写入并重新读取校验。
3. 将旧旁文件重命名为备份，再将已校验的新文件重命名为正式旁文件。
4. 校验正式文件后，确认本地日志中的对应版本已同步。旧备份在后续同步或重新打开时，确认本地记录后清理。

中断发生在切换前时保留旧文件；旧文件已移走而新文件尚未就位时恢复旧文件名；新文件已经就位但本地尚未确认时识别已完成的保存，不把它误判为外部冲突，也不会误确认更晚的本地修改。

一般写入异常会立即尝试恢复；进程被终止时，在下次打开或“重试同步”时恢复。权限失效或存储不可访问期间保留事务及副本，恢复访问后再重试。不要在未同步状态下清除应用数据，或手动删除 `.pending-…`、`.backup-…` 文件。

Android 文件提供者不承诺跨文件重命名的全局原子性，因此这是可恢复的文件切换，不是对任意云盘、存储硬件断电或多设备同时写入的绝对原子保证。切换瞬间正式文件名可能暂时不存在；只有显示已同步后才应复制文件。不支持重命名的提供者会拒绝安全替换并保留本地数据，需使用支持重命名的目录，不会退回清空旧文件的方式。

## 格式与兼容性

- `.graspfolio` 版本 2 包含原有矢量笔迹、PDF 内容哈希及 `reading`（从 0 开始的物理页码 `page`、封面独页开关 `cover`）。
- 版本 1 仍可读取。只有存在旧本机阅读设置而导入文件没有这些设置时，才迁移旧本机值；导入的版本 2 设置优先。
- 本地恢复快照和新增增量记录升级到版本 2，仍能恢复旧版日志；阅读设置修改与笔迹使用相同的版本确认机制。
- 旧 APK 不支持版本 2，应同时升级两台设备，不要降级后编辑新版数据。
- 原始 PDF 始终不改写。
