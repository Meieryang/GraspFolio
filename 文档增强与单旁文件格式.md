# 单旁文件、空白页与文字选择

## 便携格式

新写出的 `.graspfolio` 为 ZIP 容器，包含 `document.json` 及按 SHA-256 去重的 `audio/<hash>.audio`。音频逐块读写，不转为 Base64，也不把整段音频放进内存。播放使用应用私有缓存，移动书籍时只需 PDF 和一个旁文件。

仍支持读取原有 JSON 旁文件和外置音频附件。先校验、恢复本地，再通过原有暂存、改名、校验、日志确认流程升级；载入旧旁文件后自动安排格式升级。缺少或损坏音频时保留旧数据并报告失败。确认保存后清理事务备份；旧共享音频仅在目录内所有正式旁文件均已成为容器、本地恢复副本校验通过时清理，避免影响尚未迁移的书籍。

## 空白页

空白页位于阅读顺序中，PDF 本体不变；横屏在当前最右侧可见页之后插入。按钮在画笔栏右侧，点击后使用玻璃确认对话框；取消不修改数据。确认后跳转到新页。

页面标识与阅读位置分离：原 PDF 页继续使用原索引，新增空白页使用从原页数开始的递增标识。结构保存到 reading.blanks（新增页标识、前置页标识）；同一页的笔迹和音频不随插入移位。空白页沿用前置页尺寸。包含空白页的索引版本为 4，旧索引 1–3 继续兼容。

## 文字选择与翻译

文字选择按钮紧邻插页按钮，默认关闭。启用后只处理手指长按：PDF 原生文字选中后高亮，继续拖动可扩展同页范围，再点选中文字出现复制／翻译小框。关闭模式不安装文字手势处理器；笔仍用于书写，不进行 OCR。

使用 Android 15 及更新版本的 PdfRenderer.selectContent，后台读取、及时关闭页对象，取消与过期请求不能修改当前选择。较旧系统提示不支持文字选择。

翻译使用已在 iQOO Pad2 Pro 查实的 vivo 文本处理入口（ACTION_PROCESS_TEXT），优先 vivo 翻译，其次 vivo 系统翻译／浏览器翻译；不跳转到其他厂商。复制与翻译只在用户按下相应按钮时执行。

## 验证记录

2026-09-14：Debug 构建、69 项 JVM 测试及 lint 通过。iQOO Pad2 Pro（Android API 36）通过音频容器往返、旧 JSON 与外置音频迁移、真实阅读界面插页确认及文字选择测试；迁移测试在删除私有音频缓存后仅从旁文件恢复音频。此前批注日志、同步协调与加载回归共 20 项通过。

实机截图确认玻璃确认框无跨窗口取样色块；vivo 翻译入口成功显示译文。文字选区在页面位置改变时清除，避免翻页或旋转后残留。UI 测试等待文字开关实际生效后再注入手势。

旧书逐本打开后迁移，尚未打开的旧书及仍被它们使用的共享音频附件暂时保留。测试使用独立样例，没有批量改写用户书籍。
