# MVP6：图书库、扫描与最近打开

## 范围

- 保留现有图书封面、页码进度和书名搜索。
- 主页侧栏在“图书库”下增加“最近打开”，按最近成功打开时间倒序展示，重复打开移至首位，不生成重复卡片。搜索对当前标签生效。
- 设置增加“图书库扫描文件夹”：选择/更换文件夹、显示路径、移除扫描路径、重新扫描及扫描状态。
- 通过 Android 系统目录选择器取得持久读取授权，递归扫描该目录及子目录中的 PDF。扫描只读取目录条目，不复制、移动或修改源文件。
- 单独打开的 PDF 继续保留在图书库；相同提供者与文档 ID 的直接 URI、目录 URI 会去重，并优先保留既有直接 URI，避免改变其本地批注身份。
- 启动主页、从阅读返回和应用恢复前台时刷新；重新扫描会反映源文件新增、移除与改名。扫描在 IO 线程执行，封面按可见卡片加载，最多两个并发渲染。

## 行为边界

最近打开最多保存 100 本，记录保存在本设备；第一次升级无法准确推断旧版书籍的打开时间，因此不伪造历史顺序。仅扫描不会产生最近记录。已被移走或失去权限的历史条目继续保留，点击时由阅读器显示无法打开的提示。

移除扫描路径不删除文件、最近记录或旁文件，也不主动撤销其他功能可能仍在使用的目录授权。无法访问的目录会给出重新授权提示。单次扫描最多处理 10000 条目录项，达到限制时显示已找到的结果并提示选择更具体的目录。

本阶段针对高分辨率平板横、竖屏验收，不新增全盘文件权限。原有旁文件目录授权仍独立管理；扫描路径设置不自动授权批注覆写。

## 验证

- 构建、JVM 单元回归及 Android lint。
- SAF 设备测试：多级子目录、扩展名大小写、非 PDF 排除、增删后刷新、扫描不产生最近记录、最近去重排序和持久恢复、移除路径不删除文件、无效路径报错。
- 实机核对“最近打开”侧栏位置、搜索结果、空状态以及设置路径控件。
