# 全书批注预加载与安全保存

## 当前行为

按最新需求，打开文档时读取全部页面尺寸、恢复并同步批注，再逐页解码全书笔迹，建立按页组织的内存缓存。初始阅读画面准备好后才显示阅读界面。高清 PDF 画面仍按需渲染，不把全书页面位图放入内存。

加载面板使用玻璃样式，显示当前阶段、进度条和返回按钮。页面信息显示已读取页数；全书批注显示已加载笔迹条数；身份校验、旧数据恢复、旁文件及附件校验使用不定进度。计数由实际工作回调驱动，不使用模拟百分比。

翻页直接查询内存中的可见页笔迹，不再读取批注内容文件或执行 JSON 解码。新 PDF 画面未准备好时保留旧页及其笔迹，暂时禁止书写；新画面、页面位置和对应笔迹在同一 UI 更新中发布。横竖屏切换也复用全书批注缓存。

## 保存与兼容

本地继续使用 v4 索引、不可变笔迹内容文件和原子增量日志；外部 `.graspfolio` 继续兼容 JSON v1/v2/v3，阅读进度和音频引用保留。同步导入、导出及安全替换仍采用流式处理，不构造整本书的大 JSON 字符串。

编辑立即更新对应页内存缓存，然后进入本地保存队列。保存只替换修改页，保留其他页面。双页模式分别维护每页的笔迹顺序。即使本地保存失败后继续翻页，未成功保存的各页修改也会留在待保存集合中，后续编辑或重试一起提交，不能因换页而遗漏。

进度更新不重新加载全书批注。手动同步期间暂停阅读，释放旧阅读缓存，完成同步与全书加载后再恢复。正常关闭文档会释放阅读缓存，已经排队的持久化工作继续完成；本地保存失败时保留协调器及未保存修改供重试。

## 取消与失败

打开过程可返回取消；预加载逐条检查取消状态，过期回调不能重新打开文档。PDF 页面信息读取同样可取消，并关闭已打开的页和文档描述符。

任何页面的批注损坏都会使全书预加载失败，提供重试与返回，不显示不完整批注。预加载检查 Java 堆余量，低于 32 MiB 与最大堆 10% 两者的较大值时停止；另捕获加载阶段的内存分配失败。原数据不受影响，也不会自动退回分页模式。此保护不能替代实际内存容量，大量笔迹仍可能无法全量加载。

## 验证用例

- `ResidentInkTest`：2000 页、1,024,000 个采样点全量加载；400 次双页查询不增加批注文件解码次数；跨页编辑、擦除与恢复；取消和内存不足不能发布部分缓存。
- `ReaderLoadingTest`：使用真实 PDF 阅读界面验证加载前没有画笔视图、进度面板、PDF 与笔迹页码匹配、连续翻页及横竖屏切换零新增批注解码，以及加载中返回不重新打开。
- 保存协调器及日志测试：损坏远页阻止打开、修复后重试、双页新增笔迹顺序、快速跳页保存、退出恢复、跨设备旁文件及音频引用保留。

2026-09-14 最终验证：构建、63 项 JVM 单元测试与 lint 通过；包含旋转会话保留修复的 APK 已覆盖安装。相关实机用例分批通过，最后一批 4 项包括真实阅读界面加载/取消、连续翻页、旋转缓存保留、内存不足处理和百万点预加载。

测试启动曾停在系统窗口服务 `IWindowManager.getCurrentAnimatorScale`。先建立前台窗口再运行测试后，加载与旋转用例均通过；这属于测试启动环境的调整，没有放宽验收断言。

前台测试结果（2000 页、1,024,000 个采样点）：

- 批注预加载：3160.13 ms。此项不包含测试文档生成、PDF 身份校验、页面信息读取与首屏渲染。
- 400 次双页内存查询：合计 2.307 ms，批注文件解码计数没有增加。
- Java 堆采样最大约 120.81 MiB，加载前约 68.51 MiB。采样包含测试进程当时的其他对象，不是批注缓存净占用、进程总内存或系统级峰值。
- 简单 PDF 连续翻页的渲染及状态提交约 6.20–12.06 ms；从测试发送触摸命令到观察到可书写约 66.88–123.38 ms，包含命令与轮询开销，不能解释为实际屏幕帧延迟。
- 旋转后页面与笔迹匹配，批注文件解码计数不增加。

早先未固定前台的测试出现约 100.58 秒预加载，不作为正常前台性能基准。复杂 PDF、更多采样点或不同存储状态仍会改变实际打开时间。

### 移动文档后的首次旁文件导入（2026-09-14）

本地日志新增 `contentRevision`，记录最近一次笔迹或音频内容修改的版本；单独保存阅读页码不推进此字段。只有尚未关联旁文件、从未编辑内容且笔迹和音频均为空的新记录，才允许在页码未同步时导入已有旁文件。导入提交仍校验快照版本，原有外部文件校验、原子保存及真正冲突保护保持有效。写后全擦除仍被视为内容修改。

旧日志没有此字段时保守使用其 revision，不根据“当前为空”推断历史上从未编辑。已确认仅有页码的个别旧记录，应在完整备份及停用其写入协调器后定向恢复，不能批量解除保护。
