# 书写预测与闪烁验证

## 设置与绘制

阅读设置保留笔迹预测开关，并增加两个持久化选项。未设置或未知配置默认使用稳定跟手；每笔落笔时固定模式，下一笔才采用新选择。

- 稳定跟手：12 ms / 12 dp 上限，保留 SDK 有效尾迹原长度，对应首次试用版本的增强档。
- 更强预测：对通过方向检查的 SDK 尾迹使用 1.5 倍增益，同时限制在 18 ms / 18 dp 以内。

两档均拒绝无效、反向、横向跳变及过期预测；转弯、减速时缩短尾迹。保持 SDK 默认调用参数，不使用未经确认的原生倍率参数。预测始终仅作显示，不写入文件或参与擦除、套索。书写诊断显示 SDK 有效率、采用率、过滤原因和尾迹长度。

## 闪烁处理

AndroidX 的取消是尽力取消，正在执行的绘制仍可完成。前缓冲重置改为与笔画更新同队列的空场景刷新，避免取消、清除任务与新笔画竞争；场景代次在绘制线程更新，过期交接不能移除新场景。暂时保留完整场景重绘；是否改为双缓冲提交取决于真实书写的进一步复现。

新增 debug 专用 PenPreviewActivity，所有笔迹仅存在内存，不访问用户文档。FrontSurfaceTest 对实际合成屏幕取样，覆盖正在书写的长笔画和连续下笔；软件场景测试继续检查预测移除、等待交接的笔迹和透明度。

## 已验证与边界

首次两档版本的 18 项平板测试通过，包括实际屏幕取样。真实笔迹日志确认 vivo SDK 已启用，存在有效预测以及反向、慢速等过滤；不能把有效返回率等同于采用率。

屏幕取样未发现笔画丢失，不足以排除偶发闪烁或单帧撕裂。回调 CPU 耗时不是笔尖到屏幕的物理延迟；没有高速摄影时不作此承诺。增强档的主观跟手与急停修正仍需实际书写反馈。

用户反馈：首次时序修复版本暂时未再出现书写闪烁，并要求将原增强档提升为稳定档。因此采用上述 12/18 ms、12/18 dp 参数及增强档 1.5 倍增益。最终参数版本 66 项 JVM 测试及 lint、构建通过，已覆盖安装；仍保留原始采样与预测显示分离。
