# MVP3：压感书写与批注保存

## 使用

1. 在 Android Studio 运行 `dev` 分支；打开 PDF 后直接用手写笔书写。手指不生成笔迹，仍可操作书角翻页。
2. 按系统返回键打开简版菜单，切换压感笔或整笔橡皮，再点“返回沉浸阅读”。再次按返回键也可关闭菜单。
3. 书写振动默认开启，菜单中的开关会保存。实际振动仍取决于笔连接、固件和系统手写笔振动设置；应用不会替用户修改系统设置。
4. 点“授权 PDF 所在目录”，选择直接包含该 PDF 的目录，不是它的上级目录。批注写入同目录同名 `.graspfolio`，原 PDF 不修改。Android 不允许授权的目录请先通过文件管理器将 PDF 移到可授权的文档子目录。
5. 未授权仍可书写，批注先保存在应用私有目录。菜单显示同步状态，可以重新授权或重试同步。卸载应用/清除数据会删除尚未同步的本地副本，应先确认“已同步到 PDF 旁”。
6. iQOO Pencil3 双击笔身切换笔和橡皮；支持侧键的其他笔按住主键落笔临时擦除，抬笔恢复。双击回调仅在前台可书写且没有进行中的笔画时处理。

## 数据与安全边界

- `.graspfolio` 使用版本 1 JSON，包含 PDF 内容 SHA-256 标识、物理页索引、笔画 ID、真实采样点/压力/时间、笔刷类型、颜色和宽度。预测点不保存。
- 内部恢复副本使用 AtomicFile；同一文档的保存和同步在串行队列中执行，旋转或退出阅读不取消已排队的保存。
- 同名但不同内容的 PDF、未知批注版本、损坏文件、外部冲突、已删除的原旁文件都会阻止覆盖。不要通过删除应用数据来解决冲突。
- SAF 提供者不保证旁文件原子写入。中断写入造成旁文件损坏时，内部恢复副本仍保留，但不会自动覆盖损坏文件；应先保留两份数据再人工处理。当前没有自动冲突合并功能。
- 手写笔落笔时终止翻页并屏蔽手指翻页；没有实现硬件级掌拒。每笔属于起始页面，越界显示裁剪，不跨页连接。
- SDK 仓库仅解析 `vivo` 组；当前 1.0.0.10 已不申请 QUERY_ALL_PACKAGES，应用仅声明两个服务包可见。SDK 不支持的设备仍走标准 Android 手写笔输入。

## 自动验证

运行：

```powershell
.\gradlew.bat :app:assembleDebug :app:testDebugUnitTest :app:compileDebugAndroidTestKotlin :app:lintDebug
```

- 单元测试：26 项，覆盖原有页面分组/书角、新坐标变换、压力范围、快速整笔擦除、边界裁剪、JSON 往返、文档身份、同步冲突策略及预测尾迹保护。
- 平板原生测试：6 项通过，覆盖笔/掌事件规范化及历史采样、手掌抬起不污染笔画、缓存绘制/清空/尺寸重建、手指不书写及取消不提交、起始页归属、快速落笔与取消擦除。使用独立 View 测试环境，不代表真实硬件笔感或完整界面交互验收。
- Compose 连续翻页界面测试：已编译，但在平板测试 Activity 启动阶段停滞，已主动终止测试进程；未取得通过结果。原生测试已从该启动流程隔离。
- Lint 无阻断错误；保留依赖更新、既有资源等非阻断警告，不在此阶段升级整个依赖栈。

## 真机验收（尚未执行）

优化调试版和测试 APK 已覆盖安装到平板，保留应用数据。无线调试重连后六项独立原生测试通过；以下真实硬件体验仍待验证：

- 轻压/重压宽度变化，快速书写没有断笔或明显延迟；预测尾迹抬笔即消失，没有保存虚构线段。
- 默认振动生效；菜单关闭振动、抬笔、取消触摸、进入后台、退出阅读后不会继续振动。
- 双击切换工具；橡皮快速扫过删除整笔，不删除其他物理页。
- 手掌先接触后落笔、笔先落下后接触手掌，都不意外翻页；抬笔后手指角落翻页恢复。
- 横竖屏切换、封面独页、混合尺寸和正方形页面的笔迹不偏移，空白占位区不落墨。
- 书写/擦除后重新打开与重启应用，批注正确恢复；原 PDF 的 SHA-256 不变。
- 选择错误目录、撤销权限、只读目录、旁文件损坏、同名不同 PDF 时不覆盖数据；恢复正常权限后重试普通同步失败。

构建通过不等于上述硬件行为已验收。当前更改未提交，也未合并到 master。

## 手写笔优化第一轮

- SDK 只接收经过规范化的单笔事件：保留真实坐标、压力、历史采样和设备来源；手掌自身的抬起/落下事件不再重置笔的预测流。
- 仅使用向前且接近运动方向的预测尾迹；急转弯、静止、陈旧时间戳、反向及横向异常点不延伸。尾迹限制为最多 6 ms 的近期运动距离且不超过 6 dp。这是保守初值，不是经过 iQOO Pencil3 实测得出的最佳参数。
- 真实笔迹不做延迟平滑。已完成笔迹使用可重建的显示缓存，新增笔画追加到缓存；当前笔迹只光栅化新增线段。每层最多 400 万像素，两层约 32 MB；缩放/页面变化时由矢量数据重建，不改变保存格式。
- 返回键菜单新增“笔迹预测”开关，设置持久化；书写振动仍默认开启。菜单显示 SDK 服务状态、预测尾迹接受次数和绘制耗时。
- Logcat 标签为 `GraspFolioPen`，仅输出状态和每笔汇总，不记录书写坐标。CPU 绘制耗时与采样到绘制时间不是笔尖到显示面板的端到端延迟。

对比方法：同一 PDF 页面先开启预测，连续写小字、折线和圆圈；关闭预测后重复，并分别尝试手掌先落下和笔先落下。记录是否仍跳动以及菜单中的 SDK 状态。预测开启但服务尚不可用时仍只绘制真实采样，不能据此判断预测算法的效果。

重新连接设备后，可运行限定测试（不运行阅读进度存储测试）：

```powershell
adb shell am instrument -w -e class io.graspfolio.app.PenPipelineTest,io.graspfolio.app.StylusInputTest io.graspfolio.app.test/androidx.test.runner.AndroidJUnitRunner
```

这一轮没有接入前缓冲渲染，也没有做真实笔尖到屏幕的高速摄影或原子笔记基准对比，因此不宣称已达到原子笔记的延迟与笔感。

## 低延迟路线：步骤 1 / SDK 单变量升级

- 依赖从 `vivo:penengine-simplify:1.0.0.7` 升级至 `1.0.0.10`，保留现有 View/显示缓存绘制及 6 ms、6 dp 预测保护上限。
- 继续调用单参数 `computeEstimatePoint(event)`，不调整新版 `predictRate`，不叠加其他预测器。本步骤尚未引入前缓冲渲染。
- 新版 SDK 已移除广泛包查询权限，因此删除不再需要的 manifest 移除标记；应用最终权限范围不变。
- 构建、Lint、26 项 JVM 单元测试通过。平板运行 7 项测试全部通过，包括新版引擎初始化/有效坐标调用测试及此前六项原生输入测试，没有跳过。模拟输入不能替代实际笔感和物理延迟测量。
- 调试 APK 已覆盖安装并保留数据；用户的图标和文件整理改动未回滚。未提交或合并分支。
- 对比时保持相同 PDF、屏幕方向和预测开关，画快速长直线、小字、折线和圆圈；记录尾迹追赶是否改善、跳变是否回归。先记录这一版的结果，再切换前缓冲版本，以免混淆两个变量。
- 下一步：为实时笔迹接入前缓冲渲染，保留已完成笔迹及 PDF 的常规显示，再进行同条件对比。

## 低延迟路线：步骤 2 / 实时笔迹前缓冲

- 使用 AndroidX `graphics-core:1.0.4` 的 `LowLatencyCanvasView`，只将当前书写和待交接笔画放到独立渲染线程；PDF、已完成笔迹缓存和保存模型不变。SDK 保持 `1.0.0.10`，预测保护参数不变。
- 每次只向渲染线程发送不可变的新增采样副本；预测旧区域清除后重绘相交真实笔迹，预测不保存。原物理页裁剪继续有效。
- 抬笔后临时保留完成笔画，普通画面提交回调到达后才移交；快速连续落笔分别交接。取消、页面变化、菜单和后台清理实时层。API 29 以下、初始化未就绪或绘制失败时使用常规绘制。
- 返回键菜单增加持久化的“低延迟前缓冲”开关，默认开启；对比时固定预测开关、PDF、方向和笔速。橡皮继续走原有整笔擦除路径。
- Debug 构建、26 项 JVM 测试通过；Lint 为 0 errors、35 warnings、1 hint（不宣称零警告）。平板限定测试 10 项全部通过，其中新增 3 项测试覆盖预测残影清除、连续笔画独立交接/取消、页面裁剪与场景重建。这些 Canvas 场景测试不等于显示合成器的无闪烁验收。
- APK 已覆盖安装，保留数据。真实运行日志确认前缓冲 Surface 为 2064×3096、vivo 预测已启用；前两笔回调数为 133/100，回调 CPU 平均约 0.15/0.08 ms，采样至回调最大约 9/10 ms。这不是端到端屏幕延迟或与原子笔记的性能结论。
- 仍需实笔确认：快速直线追赶感、折线/圆圈稳定性、抬笔交接是否闪烁、快速连续笔画、菜单/后台/旋转后是否残影，以及前缓冲开关对比。未执行高速摄影；本轮未重新执行 Compose UI 回归测试。未提交或合并，仍在 dev。

### 步骤 2 修复：落笔后旧笔迹隐身

- 用户反馈：实时笔迹迅速消失、交叉处重现、抬笔后整笔恢复。上一版仅绘制脏区域，依赖前次硬件提交后的像素保留；这一假设与该设备上的现象不符。既有测试复用软件 Bitmap，未覆盖内容不保留的情况。
- 改为每次回调清空临时层并完整重放当前笔画及尚未移交的笔画，包括无新采样的重复回调。不重绘 PDF 或已保存批注，不改 SDK、预测参数或存储格式；保留前缓冲路径。
- 新增回归测试：每次绘制前清空输出，验证抬笔前起始线段持续可见、无新采样时仍可恢复场景、连续笔画待交接内容保留、预测尾迹不并入完成笔迹。
- 修复版 Debug/测试 APK 构建及 26 项 JVM 测试通过，Lint 无错误。ADB 恢复后已确认构建与源码一致，覆盖安装应用及测试 APK，保留数据；在平板运行 11 项限定测试全部通过，无跳过，包含新增的无上一帧内容保留测试。这是场景与输入回归测试结果；实际隐身问题是否消失及长笔画性能仍需实笔确认，不能视为屏幕显示验收完成。仍在 dev，本次未提交或合并。

### 抬笔交接和锐度修复

- 用户确认隐身消失且低延迟达到预期，但抬笔闪烁、笔迹略变模糊。
- 删除抬笔时 `LowLatencyCanvasView.commit()` 的第二套 HWUI 位图交接；实时层继续显示，直到父视图已完成笔迹帧提交后才移交/清除，避免同时走库内位图和应用缓存两条路径。取消时有待移交笔画也遵循相同路径，不改变预测参数。
- 显示缓存取消 400 万像素降采样和插值放大，改为屏幕像素 1:1；解决已完成笔迹的缩放锐度损失。2064×3096 时单层约 24.4 MiB；备用实时缓存仅常规绘制时分配，进入前缓冲笔画时释放其引用。旧位图由系统回收，不主动 recycle 仍可能被显示列表引用的位图。此前“每层最多 16 MB”说明不再适用。
- 新增高分辨率回归测试：2100×2000 缓存中的分数坐标、压力变化斜线与实时场景直接绘制的采样区域逐像素一致。此测试使用软件 Canvas，不宣称 GPU 与 CPU 抗锯齿或屏幕显示完全一致。
- Debug/测试 APK 构建、26 项 JVM 测试和 Lint 检查通过；平板 12 项限定测试全部通过，无跳过。修复版覆盖安装，保留数据。仍需实笔确认抬笔闪烁及锐度变化，尤其快速连续点划和长笔画；未做高速摄影。本轮仍在 dev，未提交/合并。

### 保存减负与性能观测

- 每笔在独立本地队列写入 AtomicFile 增量记录（新增/变更笔画、删除 ID、版本及同步基线），不再每笔序列化整份文档。每次原子提交成功后才推进可恢复版本；失败保持未同步提示，重试可补写仍在内存中的最新笔迹。
- 兼容旧的恢复 JSON；旁文件版本仍为 1、PDF 不修改。增量目录与原恢复文件相邻。累计 128 条日志后在空闲任务中压缩为原子快照，先提交快照再清理已覆盖日志；离线也压缩。缺失基线、断号、损坏或身份不匹配时禁止加载覆盖。不能直接降级至不识别增量日志的旧 APK，需先确认旁文件已同步并备份数据。
- 旁文件独立队列串行同步，停笔约 2 秒、连续修改约 5 秒触发（本地排队或已有同步会延后）；同一批修改合并成最新持久化快照。在途同步不阻塞后续本地保存；按快照版本确认，旧结果不清除新修改的未同步标记。不自动无限重试外部错误。
- 目录身份检查、同名 PDF 隔离、旁文件变化/缺失冲突保护及写后校验保留；已知哈希相同的旁文件不再重复构造 JSON 对象。退出阅读、切换文档、进入后台仅请求异步刷新，不在主线程等待文件系统。
- 前缓冲仍输出完整场景、原生分辨率与预测参数不变。每笔只进行一次页面变换/裁剪；纯前缓冲 MOVE 不再主动使父视图无效。暂未重做分块缓存或 RenderNode 稳定段缓存，避免扩大变量。
- 新增 GraspFolioPerf 日志及 Trace 分段：local_recovery、local_delta、local_ack、local_checkpoint、sidecar_encode、sidecar_total、committed_cache。记录版本、字节数、排队耗时和队列计数，不记录文档路径或笔迹坐标；阶段耗时日志只打印至少 4 ms 的样本，不能直接拿这些样本计算所有操作均值。
- 构建、28 项 JVM 测试通过；Lint 0 errors、35 warnings、1 hint。平板 20 项限定测试通过，其中新增 6 项日志测试和 2 项真实协调器测试（模拟远端阻塞/失败，使用隔离测试目录），验证慢同步不挡本地落盘、旧确认不误报同步、12 次修改合并一次同步，以及恢复/擦除/迁移/损坏保护。新增单笔记录在已有 10000 采样点时仍小于 1 KB（测试的小笔画）。
- 真实样本：本地记录约 1.3～4 KB、约 4～5.5 ms，队列等待 0 ms；旁文件约 190～230 ms，其中整文档序列化约 85～89 ms，现运行于独立同步线程。这些是少量样本，不是完整性能基准或零卡顿结论。仍需同文档连续书写、长笔画、擦除、重开恢复的体验验收。原始长笔画全量重放和大位图更新仍是后续候选优化。
- 测试版覆盖安装保留数据；本轮仍在 dev，未提交或合并。

### 密集笔画橡皮擦优化

- 原热点：每个历史/当前采样扫描全部文档笔画；strokeHit 用 zipWithNext 创建临时线段列表；每次命中把整页缓存标记失效，清空并重画全部剩余笔画。
- 新增物理页坐标空间索引（64 页面单位网格），只对轨迹附近候选做原有精确整笔命中。长距离快速扫过按完整轨迹包围区域查询，单点笔迹与页面隔离保持；超大范围走保守回退，不遗漏候选。不可变笔画的包围盒在增删时复用；线段检测改为索引遍历，无 zipWithNext 列表。
- 擦除预览和 CANCEL 恢复只重绘受影响区域及相交的可见笔迹，UP 删除后不重现。普通书写保留追加缓存路径；布局/尺寸变化仍完整重建，保持原生分辨率。
- 实测发现局部 Canvas 裁剪及改变离屏原点会影响软件抗锯齿像素，因此使用与主缓存同尺寸同原点的临时缓冲完整画候选，再以 SRC 复制受影响区域。缓冲首次需要时分配并复用，2064×3096 下额外约 24.4 MiB，视图尺寸改变或释放时丢弃引用；不降低锐度。GPU 上传仍使用原来的整张位图，本轮不宣称已消除上传成本。
- 新增 3 项 JVM 索引测试：随机轨迹与全扫描结果一致、3000 条分散笔画的局部查询仅返回 1 条候选、超大笔画回退及页面隔离。新增 2 项设备缓存测试：交叉笔迹局部擦除/CANCEL/UP 逐像素与整页重画一致、布局改变重建；小范围擦除重绘面积小于测试视口的 10%。这不是任意密集场景的固定加速倍数。
- 最终 Debug/测试 APK 构建、31 项 JVM 测试、22 项设备限定测试通过；Lint 0 errors、38 warnings、1 hint。测试版已覆盖安装并打开，保留数据；仍需在实际密集页面验证连续擦除流畅度。大量重叠长笔画时仍需处理较多候选。日志新增 erase_index、erase_hit、erase_cache_repair、committed_cache_append 耗时阶段。本轮仍在 dev，未提交或合并。

### 长笔画抬笔前停止更新

- 用户澄清：约 20 cm 后实时显示卡顿直至不可见，但抬笔后完整笔画出现。未取得对应故障日志，不能认定具体 GPU 异常；代码中确认有整笔绘制指令反复录制与逐输入刷新积压风险。
- 硬件 Canvas 路径将真实笔迹按 128 采样点记录为稳定 RenderNode 段，后续复用稳定段，仅重录不足 128 点的尾段；预测仍单独绘制，不缓存进真实笔迹、不保存。软件/旧系统保留直接绘制；每次仍输出完整可见场景，不依赖上一帧像素保留。
- UI 侧限制一个在途渲染请求及一个待刷新标记，所有采样更新仍按序发送；完成回调驱动下一次刷新。取消/切页后用代次隔离旧完成回调。未改变 SDK、预测保护上限、存储格式、整笔擦除或原生分辨率。
- 新增 2 项 JVM 帧控制测试，以及 1 项平板 RenderNode 录制压力测试：初始 12000 点后再逐点增加 256 点，每次重新记录不超过 128 段；重复无新增帧、抬笔交接、取消和释放均通过。稳定段数量和 GPU 回放成本仍随笔画长度增长，不能据此宣称任意长度恒定成本。
- 最终构建、33 项 JVM 测试、23 项设备限定测试通过，Lint 无错误。压力测试验证硬件绘制指令录制与复用，不等同真实屏幕逐帧可见性验收。测试版已覆盖安装，原数据保留；需实笔持续长线/绕圈确认不抬笔期间仍跟手可见、抬笔不闪烁。仍在 dev，未提交或合并。
