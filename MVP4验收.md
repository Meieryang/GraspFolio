# MVP4：笔刷选择栏与多画笔

## 范围与使用

- 参考 `UI_designedPhotos/阅读模式-菜单模式.png` 和 `竖屏-阅读模式-菜单模式.png`：顶部悬浮圆角工具组、雾蓝配色、半透明面板、底部细轨页码滑条。控件覆盖在页面上，不挤压 PDF 显示区域。
- 系统返回键打开工具条；点击笔或下拉箭头打开画笔面板。选择压感笔、等宽笔、荧光笔，以及六种颜色和粗细。点击“开始书写”或右上角关闭按钮恢复沉浸阅读。
- 每种笔分别记住颜色和粗细；压感笔/等宽笔为 0.5–8 PDF 点，荧光笔为 6–32 PDF 点。笔身双击仍切换当前笔与整笔橡皮，不重置选中的笔刷。
- 返回键优先关闭画笔/设置面板，再次返回关闭工具条。右上角设置中保留封面独页、书写振动、预测、前缓冲、授权目录、重试同步、诊断和退出阅读。
- 底部进度条松手后跳页并保存进度；横屏仍按原有封面/双页规则显示。
- 原 PDF 不修改。笔刷类型、颜色、宽度和真实采样继续保存在 `.graspfolio` 及内部恢复副本中，旧版压感笔批注可继续读取。
- 不包括参考图中的音频、图书库、套索或文本工具；这些不属于第四阶段的白皮书范围。

## 绘制与兼容性

- 压感笔、等宽笔沿用原有前缓冲和长笔迹分块记录；每一笔在落笔时固定样式，预测轨迹与最终笔画使用相同配置。
- 荧光笔以约 30% 不透明度整笔合成，同一笔交叉不重复加深，不同笔叠加可加深。使用单次合成的增量缓存路径，避免半透明前缓冲与已保存图层交接时短暂叠色；它不具备压感笔的前缓冲低延迟路径，实际笔感需单独体验。
- 短荧光笔画只在笔画包围区域创建临时合成层；擦除仍使用空间索引及局部缓存修复。
- UI 使用渐变、边缘高光和软阴影模拟参考图的轻玻璃外观，没有引入全屏实时模糊/折射库，避免干扰书写性能。
- 旁文件仍为版本 1，增加 `fineliner`、`highlighter` 两个笔刷标识。旧 MVP3 应用不支持这些标识，会拒绝读取，不应降级后继续编辑。未知笔刷与错误文档身份仍拒绝加载，不能静默丢弃。

## 验证记录（2026-09-08）

- Debug 构建和单元测试通过：37 项，包括新增的笔刷宽度规则、混合笔刷序列化、旧版批注读取、未知笔刷拒绝以及宽荧光笔擦除。
- 已连接 iQOO 平板原生仪器测试：27 项通过，包含新增的 4 项多画笔渲染测试，以及原有低延迟场景、长笔迹、擦除、保存恢复和输入隔离回归。
- 缩小透明合成区域后，4 项多画笔渲染测试再次通过：抬笔前后像素一致、单笔交叉透明度、前后绘制路径一致、混合笔刷擦除与取消恢复。
- 新增的 2 项 Compose 菜单测试已编译，但设备测试 Activity 启动停滞，已主动停止；不计为通过。现有 Compose 连续翻页测试也有同样的启动限制。
- 改用 Debug 专用 `ReaderToolsPreviewActivity` 进行实机截图与交互核对，不读取或修改用户 PDF。已检查横竖屏工具条和设置/画笔面板，并实际操作等宽笔、颜色、粗细及返回关闭面板。
- 预览 Activity 仅在 Debug 构建中存在，所有选项为内存状态，不保存到真实笔刷偏好；截图位于忽略的 `app/build/ui-check/`。

## 仍需使用者验收

1. 在真实 PDF 上使用三种笔书写，重点体验荧光笔的速度和透明度；连续长笔画、自交叉及快速抬落笔不应出现隐藏或闪烁。
2. 混合笔刷整笔擦除后旋转、翻页、退出重开，确认颜色、粗细、页归属和擦除结果一致。
3. 真笔双击切换橡皮后再切回，确认仍为之前的笔刷；确认振动、掌触隔离和角落连续翻页不退化。
4. 对照参考图反馈面板大小、透明度、位置和颜色。独立预览通过不代表真实 PDF/手写笔完整交互验收完成。

本阶段在 `dev` 开发；未自动提交、切换主分支或合并。
