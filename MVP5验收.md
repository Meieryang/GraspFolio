# MVP 第五阶段：页面音频与录音

## 操作入口

- 菜单左上角的耳机加号用于添加音频，通过紧凑下拉栏选择导入文件或开始录音。双页阅读时先选择关联的物理页。
- 当前页的音频按插入顺序显示为波形圆形按钮，菜单与沉浸阅读均可播放。首次点击直接播放，不展开控制栏；播放中再次点击同一条才展开暂停、进度拖动、时长和收起控制。
- 菜单中的播放面板可移除音频关联，需确认；翻到不包含该音频的页面后停止播放。
- 录音首次使用申请麦克风权限，录音条显示关联页、计时和结束保存按钮。进入后台或退出阅读自动结束并尝试保存；不会在后台继续录音。
- 音频失去焦点、耳机断开或应用进入后台时暂停，不自动抢回播放。

## 布局

参考 `UI_designedPhotos/阅读模式-菜单模式.png` 与 `UI_designedPhotos/竖屏-阅读模式-菜单模式.png`：左侧圆形音频入口、下方玻璃播放控件，沿用现有 Liquid Glass 组件。本阶段仅以高分辨率平板横屏、竖屏为验收目标。沉浸模式中有音频才显示入口，避免覆盖顶部画笔。

菜单和沉浸阅读翻页区同步将边长缩小至原来的 75%：基础 69dp，菜单仍保持 1.5 倍，即 103.5dp。仅书页外侧上角可翻页，书脊区域继续排除；移出角落取消以及多指独立判断保持不变。

## 保存与兼容

- 文件导入后复制到本地，单文件上限 200 MB；每本文档最多 1000 条音频关联。
- AAC/M4A 录音每段上限 2 小时或 200 MB，达到上限自动结束保存。
- 页面关联写入本地增量日志，音频使用 SHA-256 不可变附件；先验证附件，再事务切换旁文件。
- 录音原文件在页面关联持久化后才删除。保存失败原文件保留，重新打开同一文档时恢复；单个损坏文件不阻塞其他录音。
- 含音频的旁文件升级为版本 3，仍能读取版本 1、2；请升级迁移两端的应用。
- 迁移需同时复制 PDF、同名 `.graspfolio` 和 `graspfolio-audio-*.audio`。详见《保存与跨设备迁移说明》。

## 自动验证

- JVM：音频格式校验、旧版兼容、页关联顺序、时长显示、缩小后的翻页边界。
- 设备：导入副本独立于原文件、附件迁移与损坏校验、音频关联随笔迹日志恢复、移除关联持久化。
- 设备：待保存录音恢复、坏录音隔离、退出阅读期间保存任务继续并完成本地持久化；使用生成音频，不采集环境声音。
- 设备：高分辨率平板横屏、竖屏的实际点击播放、暂停、进度拖动、切换音频和资源释放。

实际麦克风收音质量、蓝牙麦克风路线与系统通话中断仍需人工试听验收。
